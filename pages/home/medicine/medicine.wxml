<!--pages/home/medicine/medicine.wxml-->
<mp-dialog show="{{!medicine}}" title="你没有生病哦，不许吃药！" bindbuttontap="tapDialogButton" buttons="{{buttons}}"></mp-dialog>

<image class="maingif" src="{{slimeaction}}"></image>
<view class="image">
  <image class="im" wx:for="{{imglist1}}" wx:key="{{index}}"
    src="{{item.url}}" bindtap="click" data-id="{{item.id}}"></image>
</view>
